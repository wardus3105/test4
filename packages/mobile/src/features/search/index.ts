
/* 
    Created by longdq
*/

import SearchContainer from 'features/search/view/search.screen';
import { connect } from 'react-redux';
import { Dispatch } from 'redux';

const mapStateToProps = (state: any) => ({

})

const mapDispatchToProps = (dispatch: Dispatch) => ({
   
});

export const SearchScreen = connect(mapStateToProps, mapDispatchToProps)(SearchContainer)


