
/* 
    Created by longdq
*/

import ViewPhotoContainer from 'features/view-photo/view/view-photo.screen';
import { connect } from 'react-redux';
import { Dispatch } from 'redux';

const mapStateToProps = (state: any) => ({

})

const mapDispatchToProps = (dispatch: Dispatch) => ({
   
});

export const ViewPhotoScreen = connect(mapStateToProps, mapDispatchToProps)(ViewPhotoContainer)


