
/* 
    Created by huydz
*/

import SearchFieldContainer from './search-field.screen';
import { connect } from 'react-redux';
import { Dispatch } from 'redux';

const mapStateToProps = (state: any) => ({

})

const mapDispatchToProps = (dispatch: Dispatch) => ({
   
});

export const SearchFieldScreen = connect(mapStateToProps, mapDispatchToProps)(SearchFieldContainer)


